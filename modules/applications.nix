{ ... }:
{
  programs.wezterm = {
    enable = true;
    enableBashIntegration = true;
  };

  programs.brave = {
    enable = true;
  };

  programs.discord = {
    enable = true;
  };

  programs.opencode = {
    enable = true;
  };

  programs.tmux = {
    enable = true;
  };
}
